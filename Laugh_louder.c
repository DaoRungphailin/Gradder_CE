#include<stdio.h>
#include<string.h>
int main() 
{
    char Jane[10] , Noon[10] , Bow[10];
    int a , b , c ;
    scanf("%c %c %c" ,&Jane,&Noon,&Bow);
    a = strlen(Jane) ;
    b = strlen(Noon) ;
    c = strlen(Bow) ;
    if(a==b || a==c || b==c)
    {
        printf(" ")
    }
}